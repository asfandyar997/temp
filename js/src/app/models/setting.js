import C from './../config/constants';

/**
 * Setting model
 */
export default Object.freeze({
    id: null,
    key: '',
    group: '',
    value: '',
    type: C.SETTING_TYPE.STRING,
    description: '',
    editable: true
});
